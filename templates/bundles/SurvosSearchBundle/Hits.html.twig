{% block content %}
    <div {{ attributes.defaults({ class: 'ux-search-hits' }) }}>
        {% if results.hits|length > 0 %}
            {# One batched claims query for the whole page so each card's asset_meta() is a cache hit. #}
            {% do asset_claims_prime(results.hits|map(hit => hit.data.id ?? null)) %}
            {# Fewer columns (max 3) leaves room for the observation prose, which is all we have for photos. #}
            <div class="row row-cols-1 row-cols-md-2 row-cols-xl-3 g-3">
                {% for hit in results.hits %}
                    {% set entity = hit.data %}
                    <div class="col">
                        {# debug: relevance score — use it to choose scoreThreshold on AssetSearch #}
                        {% if hit.score %}
                            <span class="badge bg-secondary mb-1">score {{ hit.score|round(5) }}</span>
                        {% endif %}
                        {% if entity.originalUrl is defined %}
                            {{ include('asset/_hit_asset.html.twig', { asset: entity }) }}
                        {% else %}
                            <div class="card h-100">
                                <div class="card-body">
                                    <pre class="small mb-0">{{ entity|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
                                </div>
                            </div>
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        {% else %}
            {% block noResult %}
                <div class="ux-search-hits__no-result">{{ 'no_result'|trans(domain='survos_search') }}</div>
            {% endblock %}
        {% endif %}
    </div>
{% endblock %}
