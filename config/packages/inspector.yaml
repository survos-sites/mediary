inspector:
    ingestion_key: '%env(INSPECTOR_INGESTION_KEY)%'
    enabled: true
    ignore_routes:
        - '/_wdt*'
        - '/_profiler*'
    ignore_commands:
        - 'd:m:m'
        - 'messenger:stats'
        - 'assets:compile'
        - 'cache:*'
        - 'debug:*'
        - 'make.*'
        - 'asset-map:compile'
        - 'importmap:install'
        - 'fos:js-routing:dump'
        - 'messenger*'
        - 'assets*'
        - 'importmap*'
        - 'cache*'
        - 'doctrine*'
        - 'd:*'
        - 'schedule:run'
        # Bulk index builds run a query and a bulk request per page for a million assets; tracing
        # every one as a segment exhausted 1G at 155k documents in production.
        - 'elastic:*'
        - 'debug*'
        - 'make*'
