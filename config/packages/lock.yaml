framework:
    lock:
        default: '%env(LOCK_DSN)%'
        # Advisory locks guarding the shared source-host token buckets (SourceRateLimiter):
        # flock is per container, and the archive workers are four containers.
        source_rate: '%env(DATABASE_URL)%'
