survos_crawler:
# if you've run symfony proxy:domain:attach my-project, then the error messages will be displayed with this prefix.  That way, you can simply click on a link to open the page.
    base_url: 'https://media.wip/'
    user_class: App\Entity\User
    users:
#        - user@user.com
        #- dummysais@example.com
    max_depth : 3
    max_per_route : 3
    routes_to_ignore :
        - 'app_logout'
        #media is not set correctly yet
        - 'media_symfony_crud_index'
        #registration require the buolt User to have the cdoe and approx set ...
        - 'app_register'
        #temp ignore media show route
        - 'media_show'
    paths_to_ignore :
        - '/admin/media/new'
        - '/admin/media/edit'
        - '/admin/media/delete'
        - '/admin/media/preview'
        - '/admin*'
        #/tools is for mcp
        - '/tools*'
        - '/inspection*'
        - 'media/browse/*'
        - '/media/browse/*'
        - 'media/index*'
        # EasyAdmin CRUD autocomplete endpoints (ez_admin_*_autocomplete) require
        # originatingPage/propertyName context params; probing them bare 500s by design.
        - '/ez/.*/autocomplete'
